import CinematicQuests from '@/components/cinematic/cinematic-quests';

export default function QuestsPage() {
  // Cinematic reskin (staging). Legacy version preserved in git history.
  return <CinematicQuests />;
}
