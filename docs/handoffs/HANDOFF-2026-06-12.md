# Handoff — 2026-06-12

**Branch:** `main` (all work merged; local and `origin/main` in sync)
**Production:** https://learning-agent-pearl.vercel.app
**Deploy:** `npm run deploy:prod` (verifies linked project + alias). Everything below is live on `main` and in production.

This was a long UX/polish session — 30 PRs (#18–#51), each built → PR → merged → deployed individually.

---

## What shipped today

### Profile / onboarding / reset
- **Working "Reset Profile"** (#18) — was a no-op stub. Now deletes the server profile (`DELETE /api/user-data`, new `deleteUserData` in `lib/blob-store.js`) **and** clears learning localStorage (`lp_*`, `learner_*`, `ai_impact_*`, `calibration_*`, `tutorial_completed`), then hard-navigates to `/onboarding`.
- **Profile link in the user menu** (#19) — `/profile` was previously unreachable from nav.
- **Live "Welcome back" name** (#20) — homepage greeting now reads `display_name` from the live profile context (`components/welcome-greeting.jsx`), not the cached server render.
- **Profile refresh after onboarding** (#21) — `profile-provider` fetches with `cache: 'no-store'`; onboarding finish hard-navigates so server + client both re-read the new profile (fixed stale "Find AI" task chips).
- **Preserve name on re-onboarding** (#22) — onboarding no longer blanks `display_name` in demo mode (falls back to existing profile name).
- **Change role without Reset** (#36) — Profile Danger Zone now points department/team/task changes to **My Role** (keeps progress); Reset is full-wipe only. (`/my-role` already edits inline via `updateProfile`, no onboarding.)

### Sidebar / navigation (big refactor)
- **Docked left sidebar** (#26) — replaced the floating hamburger dropdown with a persistent full-height left rail (`components/sidebar.jsx`: `SidebarProvider` / `SideNav` / `SidebarToggle` / `SidebarShell`). Content shifts right on `lg`; mobile gets a backdrop.
- **Global** (#28) — sidebar moved into the root layout so it's on every page; `PageHeader` got a `SidebarToggle`.
- **Sections + Skill Shop** (#23, #40, #41) — grouped into Learn / Your Progress / Account + an **HCP Skill Shop** section with the AI Self-Guided Journey levels and the **AI Resources Home** hub (`pages/183`).
- **Item descriptions** (#42) and **section headers styled like the dashboard** (#43) — bold ink labels, icons, thin dividers.
- **My Role + My Tasks added** (#44) — menu now has full parity with everything reachable in the app.
- **Default open** (#39) — stops persisting a closed state across sessions; always opens on load (still stays open while navigating).
- **Single X** (#30) — header toggle always shows the menu icon; the sidebar's own X closes it (no more double-X).
- **Alphabetized + Dashboard link** (#48) — items A–Z within each section; sections A–Z (Account, Learn, Your Progress); HCP Skill Shop pinned last (external). Added a **Dashboard** link (→ `/`).
- **"Just Chat" label** (#35) — sidebar matches the dashboard card / chat header.
- **Dark mode moved into the menu** (#46, #49, #39) — replaced the floating Dark pill with a **Dark/Light toggle row** in Account (`MenuThemeToggle` in `components/theme-toggle.jsx`), in alphabetical order. Account section moved up (now first).

### Back navigation
- **Back arrow → previous page** (#30) — `components/back-button.jsx` uses `router.back()` (falls back to `/` with no history) instead of always jumping to the dashboard.

### Lessons
- **What+Why → Concepts → Practice** (#24) — added a `concepts` phase to the lesson prompt (`buildLessonSystemPrompt` in `lib/ai.js`) + "Key Concepts" badge.
- **Guided next-step chips** (#37) — replaced the bland "Continue" prefill with tappable suggestion chips per slide (primary Continue + engaging options like "Show me an example", "Quiz me"). Model now emits 2–3 buttons per slide; input left free for typing.

### Chat → lesson
- **Detect learning intent → offer a lesson** (#25) — `lib/lesson-intent.js` `detectLessonTopic()`.
- **Guided launch wizard** (#27) — `components/chat-lesson-offer.jsx`: offer → depth (Quick Tip / Quick Lesson / Deep Dive) → mode (Read / Watch) → launches `/lesson?topic=&format=&mode=`. Lesson page now reads `?mode=` and `?format=`. Resume via the existing banner.
- **Server-side detection** (#28) — moved to `/api/chat` (returns `lessonTopic`).
- **Help routes topic Qs to Chat/Lesson** (#34) — the help assistant points learners to Just Chat / Lesson rather than teaching topics itself.

### Narrated video lessons (bug + behavior)
- **Fixed not playing** (#38) — autoplay was blocked (audio after async fetch). Then per request, **auto-play once ready** with a watchdog that falls back to "Press play" only if the browser blocks it (#39). Pause + prev/next arrows work.

### Floating help assistant
- **New help widget** (#33) — `components/help-widget.jsx`: "Questions? Chat here" blob on every page (hidden on `/chat`). Answers platform questions; routes app issues to the **HCP Skill Shop Help** Slack channels (`C04BU29V4TH`) and **HCP MX Skill Shop Help** (`C04J8BRUJQY`), both pinned in the footer. Backed by `/api/help` + `generateHelpReply` / `buildHelpSystemPrompt` in `lib/ai.js`.
- **Moved to bottom-right corner** (#45); theme toggle was bumped up then removed entirely (now in the menu).

### Dashboard
- **"Other ways to learn" header** (#31) styled like "Find something to learn".
- **XP explainer corrected** (#47) — `components/xp-explainer.jsx` now matches the code: Chat **+5/message** (was wrongly +25), plus added **Games (+25)** and **Review (+5)**. Six real sources total.
- **Restructure** (#50, #51) — Project Quests moved up into **Other ways to learn** as an (alphabetized) tile; **Your Active Projects** preview removed (lives under Account → Projects); **AI News** got its own section divider and the redundant in-card title was dropped.

### Admin
- **Lesson Content Preview** (#29) — `/admin/lesson-preview` + `/api/admin/lesson-preview` (admin-gated). Enter a topic, pick depth + tier, and it runs the lesson end-to-end (auto-advancing every phase) so admins can QA exactly what learners get. Linked from the Admin Dashboard.

### User menu
- **Alphabetized** (#32) — Learner dropdown links A–Z (Voice + Log Out stay at the bottom as controls).

---

## Parked / blocked

| Item | Notes |
|------|-------|
| **Compare yourself vs team/person** | **Deferred until Supabase.** No cross-user learning data exists — XP/skills/streak are per-browser localStorage; leaderboard is hardcoded empty (`DEPARTMENTS = []`); manager webhook returns org structure only. When Supabase lands, build this + light up the real leaderboard. An interim "you vs. your past self / a goal" view is doable today off existing data. |
| **Supabase (still pending)** | Unblocks: real leaderboard, cross-device XP, Slack `/streak` & `/skills` (mock today), and the compare feature above. |
| **Slack DM scope / SSO-Okta** | Unchanged from 2026-06-11 handoff. |
| **`/api/help` rate limit** | New open LLM endpoint (same pattern as `/api/chat`, `/api/tts`, `/api/lesson/video`). Consider a rate limit to avoid cost abuse. |

### Open decisions (low priority)
- **Chat XP**: explainer now says +5/message to match code. If you actually want +25, bump the code instead.
- **`goal_added` / `project_added` (+5)**: defined in `lib/progression.js` XP_AMOUNTS but **not wired to any action** — either wire them up or remove.
- **Orphaned file**: `components/active-projects-preview.jsx` is no longer imported (dashboard preview removed). Safe to delete.
- Chip treatment / post-grading next-steps for the **Practice** flow; **"replay this scene"** button on narrated video — both offered, not yet built.

---

## Key references
- **Production:** `learning-agent-pearl.vercel.app`. **Repo:** `skylarhoover-26/Learning-Agent-Repository`.
- **Demo mode:** Okta not configured → everyone is `demo@housecallpro.com` (shared profile). SSO will scope per-user.
- **HCP Skill Shop:** AI Resources Home `pages/183`; Journey levels `pages/214` (L1), `pages/217` (L2), `pages/218` (L3).
- **Pre-push checklist:** see `CLAUDE.md`. Deploy with `npm run deploy:prod` (never raw `vercel --prod`).
- Prior handoff: `HANDOFF-2026-06-11.md`.
